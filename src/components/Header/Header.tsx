const Header = () => {
  return (
    <header className="flex justify-between items-center absolute top-0 left-0 w-full bg-transparent p-4">
      <a href="#">lyria</a>
    </header>
  );
};

export default Header;
